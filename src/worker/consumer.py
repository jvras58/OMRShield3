"""
worker/consumer.py — Consumidor de fila de lote via FastStream (placeholder).

Instale FastStream quando for implementar:
  uv add faststream[rabbit]   # ou [kafka], [nats], etc.

Exemplo de uso básico:
  from faststream.rabbit import RabbitBroker
  from src.services.cartao_service import ExtratorCartao
  from src.settings.config import settings
"""
